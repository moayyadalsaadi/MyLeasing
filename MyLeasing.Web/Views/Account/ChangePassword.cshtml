@model MyLeasing.Web.Models.ChangePasswordViewModel
@{
    ViewData["Title"] = "تغيير كلمة المرور";
}

<style>

    body, html {
        height: 100%;
        background-color: #F5F5F5;
    }

    .box {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%,-50%);
        width: 400px;
        padding: 50px;
        background-color: #fff;
        border-radius: 5px;
        margin-top: 25px;
    }

        .box input {
            padding: 10px 0;
            margin-bottom: 30px;
            width: 100%;
            box-sizing: border-box;
            box-shadow: none;
            outline: none;
            border: none;
            border-bottom: 1px solid #f2f2f2;
        }

        .box form div {
            position: relative;
        }

            .box form div label {
                position: absolute;
                top: 5px;
                right: 0px;
                pointer-events: none;
                transition: .5s;
                font-weight: normal;
                color:#000029;
            }

        .box input:focus ~ label,
        .box input:valid ~ label {
            top: -12px;
            right: 0;
             transition: .5s;
            font-size: 13px;
        }

        .box input:focus,
        .box input:valid {
        border-bottom:1px solid #97D7EA;
        transition: .5s;
        }
</style>

<div class="box thumbnail">

    <form method="post">
        

        <div style="margin-top:-15px;">

            <input asp-for="OldPassword" type="password" required="" />
            <label asp-for="OldPassword">كلمة السر الحالية</label>
            <span style="margin-top:45px; right:0; position:absolute; " asp-validation-for="OldPassword" class="text-danger"></span>
        </div>

        <div style="margin-top:10px;">

            <input asp-for="NewPassword" type="password" required="" />
            <label asp-for="NewPassword">كلمة السر الجديدة</label>
            <span style="margin-top:45px; right:0; position:absolute; " asp-validation-for="NewPassword" class="text-danger"></span>
        </div>

        <div style="margin-top:10px;">

            <input asp-for="Confirm" type="password" required="" />
            <label asp-for="Confirm">تأكيد كلمة السر</label>
            <span style="margin-top:50px; right:0; position:absolute; " asp-validation-for="Confirm" class="text-danger"></span>
        </div>
         <div asp-validation-summary="ModelOnly"></div>
        <div style="margin-bottom:-10px;">
            <input type="submit" value="تغيير كلمة السر" class="btn btn-info" style="margin-top:10px; border:none; outline: none;" />
            <button class="btn" style="border: none; outline: none;"><a asp-action="ChangeUser" style="color:#000; text-decoration:none;">إلغاء</a></button>
        </div>
    </form>

</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
